#include <stdio.h>
#include <math.h>

int main()
{
    int a = 5, b = 6, c;
   // c = pow(a, b);
    printf("This is only the beginning, my best is yet to come\n");
    //printf("%d", c);
    return 0;
}
